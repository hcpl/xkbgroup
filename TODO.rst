* Add API methods/properties:

  - ``groups_data`` — For collecting every data available about all groups
  - ``group_data`` — For collecting every data available about the current
    group

* Add a formatting method
